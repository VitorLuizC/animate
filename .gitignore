# Unversioned files and folders.
# References at https://git-scm.com/docs/gitignore.

# Finder's configuration files (Mac).
.DS_Store

# Node.js modules.
node_modules/

# NPM's lockfile.
# We're using PNPM and it provides its own lockfile.
npm-lockfile.json

# Yarn's lockfile
# We're using PNPM and it provides its own lockfile.
yarn.lock

# Log's files.
*.log
*.log.*

# Bundled modules and their source-maps.
dist/

# Generated type declarations.
types/

# Transpiled modules and their source-maps.
src/**/*.js
src/**/*.js.map
